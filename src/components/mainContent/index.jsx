import { Container } from "@mui/system";

function MainContent() {
  return (
    <Container
      sx={{
        maxWidth: "650px",
        margin: "100px auto",
      }}
    ></Container>
  );
}

export default MainContent;
